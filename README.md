# AI-agent-pruning-engine
Custom-built AI engine featuring a 'PrunableLinear' layer architecture. This project demonstrates dynamic model compression by training a network to autonomously identify and remove redundant connections using sparsity-driven loss functions.
